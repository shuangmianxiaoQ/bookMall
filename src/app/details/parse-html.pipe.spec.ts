import { ParseHtmlPipe } from './parse-html.pipe';

describe('ParseHtmlPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseHtmlPipe();
    expect(pipe).toBeTruthy();
  });
});
